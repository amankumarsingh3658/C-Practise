#include<stdio.h>

int main()
{
    int a;int b=a; //Valid
    int v = 3^3; //Valid but not return 9 as it is bitwise XOR;
    char dt = '21'; //Invalid no more than 2 characters possible.
    return 0;
}